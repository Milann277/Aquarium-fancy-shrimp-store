{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-server",
            "type": "shell",
            "command": "python -m http.server 8080", // Ensure this matches your project needs.
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}